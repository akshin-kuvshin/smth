#ifndef __SORT
#define __SORT

void swap(int* a, int* b);
void upgraded_bubble_sort(int* arr, int len); //  с отслеживанием места последнего обмена
void shaker_sort(int* arr, int len);

#endif
